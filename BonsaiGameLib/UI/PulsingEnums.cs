using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Bonsai.Framework.UI
{
    public enum PulseChannelMode { R, G, B }

    public enum PulseSpeed { Slow = 1, Medium = 2, Fast = 4 }

    public enum PulseDepth { Deep = 50, Moderate = 120, Shallow = 200 }
}
