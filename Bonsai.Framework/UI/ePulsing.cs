using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Bonsai.Framework.UI
{
    public enum ePulseChannelSelection { R, G, B }

    public enum ePulseSpeed { Slow = 1, Medium = 2, Fast = 4 }

    public enum ePulseDepth { Deep = 50, Moderate = 120, Shallow = 200 }
}
